
if [[ -d "${rvm_path:-$HOME/.rvm}/environments" \
  && -s "${rvm_path:-$HOME/.rvm}/environments/ruby-1.8.7-p302@orange_core_dev" ]] ; then
  \. "${rvm_path:-$HOME/.rvm}/environments/ruby-1.8.7-p302@orange_core_dev"
else
  rvm --create  "ruby-1.8.7-p302@orange_core_dev"
fi
